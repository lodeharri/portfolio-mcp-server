"""Integration-test fixtures.

Currently a thin re-export of ``tests/conftest.py`` so that integration
tests have a natural place to grow domain-specific helpers (e.g.
``async_client_with_real_db`` for tests that need sqlite-vec).
"""
